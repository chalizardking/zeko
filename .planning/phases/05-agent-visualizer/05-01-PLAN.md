---
phase: 05-agent-visualizer
plan: 01
type: execute
depends_on: ["04-04"]
files_modified: [packages/eko-swarm-extension/src/sidebar/components/AgentCard.tsx, packages/eko-swarm-extension/src/sidebar/components/AgentGrid.tsx, packages/eko-swarm-extension/src/sidebar/App.tsx]
domain: ui-design
---

<objective>
Create Agent Profile components for visualizing individual agent status.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/eko-swarm-extension/src/sidebar/App.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AgentCard Component</name>
  <files>packages/eko-swarm-extension/src/sidebar/components/AgentCard.tsx</files>
  <action>Create a reusable `AgentCard` component that displays agent name, icon/color, and current status (thinking/idle/busy). Use Tailwind for styling (glassmorphism, animations).</action>
  <verify>File exists</verify>
  <done>Component created</done>
</task>

<task type="auto">
  <name>Task 2: Create AgentGrid Component</name>
  <files>packages/eko-swarm-extension/src/sidebar/components/AgentGrid.tsx</files>
  <action>Create a grid layout component to hold multiple `AgentCard`s. Handle empty states nicely.</action>
  <verify>File exists</verify>
  <done>Grid layout ready</done>
</task>

<task type="auto">
  <name>Task 3: Integrate into App</name>
  <files>packages/eko-swarm-extension/src/sidebar/App.tsx</files>
  <action>Replace the inline agent mapping in `App.tsx` with the new `AgentGrid` and `AgentCard` components.</action>
  <verify>App.tsx uses new components</verify>
  <done>UI refactored to use dedicated components</done>
</task>

</tasks>

<verification>
- [ ] Sidebar displays agent cards correctly
- [ ] Status animations (thinking bounce) work
</verification>

<success_criteria>
- Modular agent visualization components
- Clean integration into main App
</success_criteria>

<output>
After completion, create `.planning/phases/05-agent-visualizer/05-01-SUMMARY.md`
</output>

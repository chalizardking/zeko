---
phase: 05-agent-visualizer
plan: 02
type: execute
depends_on: ["05-01"]
files_modified: [packages/eko-swarm-extension/src/background/swarm/SwarmManager.ts, packages/eko-swarm-extension/src/sidebar/components/ActivityFeed.tsx, packages/eko-swarm-extension/src/background/swarm/types.ts]
domain: with-agent-sdk
---

<objective>
Implement real-time activity log/terminal for agent thoughts and actions.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/eko-swarm-extension/src/background/swarm/SwarmManager.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Activity Types</name>
  <files>packages/eko-swarm-extension/src/background/swarm/types.ts</files>
  <action>Add `ActivityLog` interfaces to `types.ts`. Include `timestamp`, `agentId`, `type` (thought, tool, result), and `content`.</action>
  <verify>Types exported</verify>
  <done>Domain model updated</done>
</task>

<task type="auto">
  <name>Task 2: Implement ActivityFeed Component</name>
  <files>packages/eko-swarm-extension/src/sidebar/components/ActivityFeed.tsx</files>
  <action>Create a scrollable terminal-like feed component. Support different styles for thoughts (dimmed), tool calls (highlighted), and errors (red).</action>
  <verify>Component exists</verify>
  <done>UI component ready</done>
</task>

<task type="auto">
  <name>Task 3: Broadcast Activity Logs</name>
  <files>packages/eko-swarm-extension/src/background/swarm/SwarmManager.ts</files>
  <action>Add `addActivityLog` method to `SwarmManager`. Emit `SWARM_ACTIVITY` messages to the frontend. Integrate with Eko's logging or callback system (if accessible) or manually log for now.</action>
  <verify>Manager emits log events</verify>
  <done>Backend broadcasting logs</done>
</task>

</tasks>

<verification>
- [ ] Logs appear in the feed when `runTask` is called
- [ ] Feed auto-scrolls to bottom
</verification>

<success_criteria>
- Real-time visibility into agent reasoning
- "Matrix-style" scrolling log UI
</success_criteria>

<output>
After completion, create `.planning/phases/05-agent-visualizer/05-02-SUMMARY.md`
</output>

---
phase: 05-agent-visualizer
plan: 03
type: execute
depends_on: ["05-02"]
files_modified: [packages/eko-swarm-extension/src/sidebar/App.tsx, packages/eko-swarm-extension/src/sidebar/hooks/useMessageBridge.ts]
domain: ui-design
---

<objective>
Connect UI to live swarm state updates and integrate ActivityFeed.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/eko-swarm-extension/src/sidebar/App.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Message Bridge for Activity</name>
  <files>packages/eko-swarm-extension/src/sidebar/hooks/useMessageBridge.ts</files>
  <action>Update hook to expose an event listener specifically for `SWARM_ACTIVITY` so the `ActivityFeed` can append logs locally without re-rendering the whole App.</action>
  <verify>Hook supports event subscription</verify>
  <done>Bridge enhanced</done>
</task>

<task type="auto">
  <name>Task 2: Integrate ActivityFeed into App</name>
  <files>packages/eko-swarm-extension/src/sidebar/App.tsx</files>
  <action>Add `ActivityFeed` to the main layout, likely below the Agent Grid. Pass the log stream to it.</action>
  <verify>Feed renders in App</verify>
  <done>UI composition complete</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Agent Visualizer</what-built>
  <how-to-verify>
    1. Reload extension
    2. Type "Tell me a joke" and hit GO
    3. Watch agents light up (thinking state)
    4. Watch activity feed populate with logs
  </how-to-verify>
  <resume-signal>Type "approved" if visualizer works</resume-signal>
</task>

</tasks>

<verification>
- [ ] Smooth animations
- [ ] Logs flow in real-time
</verification>

<success_criteria>
- Full visibility into the swarm's black box
- High "cool factor" for the user
</success_criteria>

<output>
After completion, create `.planning/phases/05-agent-visualizer/05-03-SUMMARY.md`
</output>
